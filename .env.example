# Endpoint que recebe o formulário de orçamento (Formspree, n8n, API própria...).
# Sem esta variável o formulário abre o cliente de e-mail do visitante com a
# mensagem já preenchida — funciona, mas sem registro no servidor.
VITE_FORM_ENDPOINT=""
